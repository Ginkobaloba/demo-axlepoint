# 2026-09-19 21:37 CDT - ci: make Quick Verify a real PR gate, fix D-006..D-010 collision
- **Change:** .github/workflows/verify.yml's "Quick Verify (all PRs)" job now runs npm ci, the ledger check, the new duplicate-decision-id check (scripts/check-decisions.mjs + check-decisions.test.mjs), tsc --noEmit, lint, npm test, and npm run build against the PR's own checkout, with the old live-site curl split into its own non-required "Live smoke (deployed site)" job. Renumbered docs/demos/axlepoint/decisions.md's D-006 through D-010 (accidentally reused by a later wave of decisions) to D-016 through D-020, updating every internal cross-reference and two code comments that named the old numbers.
- **Why:** Quick Verify ran no tests, no build, and no typecheck; its only step curled the live deployed site, so it was structurally incapable of failing because of a PR's changes. Porting demo-harborbistro's duplicate-decision-id checker (b205dec, PR #42) against the real file immediately found a genuine pre-existing collision that predated this change.
- **State after:** node scripts/check-decisions.mjs reports 21 unique ids, 0 problems against the real file (20 before this ledger entry's own D-021 heading was added). npm test (vitest, includes the 9-case check-decisions.test.mjs), tsc, lint, and npm run build all green in a fresh worktree with no data/ directory generated (proves the build does not need the seeded DB). Each new Quick Verify step proven able to fail on a throwaway branch; see PR body for run links.
- **Refs:** docs/demos/axlepoint/decisions.md D-021; demo-harborbistro PR #42 (b205dec); demo-slatewell PR #43 (ci/quick-verify-real-gate)
