# 2026-09-19 17:48 CDT - portal session cookie is verified, not just checked for presence
- **Who:** Claude Sonnet 5, session_01WAuS4DA21vazVZe9XMXuAw, at Drew Mattick's direction (tier-3 fix).
- **Change:** src/middleware.ts now calls readPortalSession (jose, Edge-safe subpath imports) on the axle_portal_session cookie instead of only checking request.cookies.has(). A missing, invalid, or forged portal cookie is treated as no session and is cleared on the way out. portal-session.ts switched from the jose root import to jose/jwt/sign and jose/jwt/verify so the middleware's Edge bundle does not pull in JWE. Added src/middleware.test.ts (10 tests, hostile portal cookie matrix) and src/lib/test-helpers/portal-session-tokens.ts. Decision entry D-015 in docs/demos/axlepoint/decisions.md.
- **Why:** The portal cookie path was decorative: any cookie value opened /app, and readPortalSession (the real verifier) had no production caller, the same finding class as lumen-analytics L1. The demo cookie path (axle_demo_session) stays presence-only on purpose, per D-006; this closes forged portal identity, not the demo path.
- **State after:** Full suite 111 passed, 2 pre-existing skipped, on the branch rebased onto origin/main (which now includes PR #31's ledger-check fix); 101 passed on that same base before this branch's 10 new tests. tsc, lint, and production build all green, including a clean Edge middleware bundle. Mutation check: reverting middleware.ts to the pristine presence-only check turned 4 of the 10 new tests red; restored from a pristine copy, diff clean, git status clean before commit. Not merged, not deployed as of this entry; deploy-demo.ps1 must run against the merged image for this to reach the live demo.
- **Refs:** docs/demos/axlepoint/decisions.md D-015; lumen-analytics origin/main src/middleware.ts and tests/middleware-session.test.ts (the pattern this mirrors); PR #30 (fix/customer-session-claims, branch head 311276e) adds requiredClaims/maxTokenAge/exp-iat bound to readPortalSession, deliberately not duplicated here.
